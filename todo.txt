Test for missing primary keys when doing delete, update, select (though not in the SQL Generator, but rather in the repository or whatever helper it'll have)

Schema support på repo / sql generator
Async repo
Aggregate repo
Heap repo
Fjern "Base" navngivning
evt. "GetMany<TRecord, TArgument>(TArgument arg)" og GetSingle ditto, hvor reflection bruges på TArgument til at finde samtlige properties og mappe dem op til kendte columnName/PropertyNames på TRecord. Mangler noget kan den kaste ArgumentException.



HeapRepository<TRecord> : BaseRepository<TRecord, TRecord> løser PK issue uden at skrive kode
